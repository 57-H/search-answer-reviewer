# Search Answer Review

本项目区分技能被选中、搜索结果实际接受审查，以及审查结果是否通过；三者不能互相代替。

## Language

**普通 Skill 安装**：使用者将本项目的技能文件交给兼容的 AI 助手，由助手自主决定何时加载和执行。它不控制助手实际调用的搜索工具。

**技能触发**：AI 在一次任务中加载并开始遵循本 skill 的指令。它本身不证明每批搜索结果都经过审查。

**结果审查**：AI 或人工对取得的搜索结果作出与用户需求及证据有关的判断，并留下可复查的记录。

**漏审查**：搜索结果已被用于答案或决策，却没有先完成结果审查；无论技能是否曾被触发，都属于漏审查。

**逐批审查覆盖率**：用于答案或决策的搜索结果批次中，在使用前完成结果审查的比例。技能加载率可以辅助解释覆盖率，但不能代替它。

**已审查**：AI 已对一条结果完成约定范围内的检查并记录结论；结果可以匹配、部分匹配、不匹配或无法判断。“已审查”不等于“审查通过”。

**普通模式批次记录**：普通 Skill 为一次搜索返回的全部结果保存的轻量记录，包含批次与结果身份、需求匹配、来源身份、答案使用的事实及其审查结论。用户看到的“已审查 x/y”只能由该记录计算。

**结果使用**：某条搜索结果中的信息影响了最终答案、推荐、排除理由或其他决策。运行轨迹中，结果首次使用前没有完整审查记录即构成漏审查。

**受门控搜索**：搜索结果在逐条审查完成前不会交给负责回答的 AI 的搜索方式。

**审查通过**：一条结果被判断为符合用户需求，且其关联的事实性断言均有已取得证据支持；不表示网页内容已被独立证明为现实真相。

**严重误放行**：一条会改变答案或推荐的结果，在明显不符合用户硬性要求或缺少关键证据时，被当成合格依据使用。
