# Single live-source smoke check

On 2026-09-17, the current Codex host searched and opened the [Python 3.10 `sqlite3` documentation](https://docs.python.org/3.10/library/sqlite3.html). The saved input tests the incorrect statement that Python 3.10 first added the `deterministic` parameter. The obtained page excerpt says it was new in 3.8. The report records one contradiction with an exact source-text span.

This is one host-model review, with the model identifier and token usage unavailable. The captured text is a partial excerpt; the page URL and observation time are preserved. No independent evaluator checked the review, and this example is not an effectiveness estimate. `request.json`, `judgments.json`, `review.json` and `review.md` preserve this particular run; `replay` will relabel the reviewer `saved_demo` because a replay does not call a model.
